# AI agent skill

Shoulders ships an agent skill teaching AI assistants to deploy and manage apps.

Install globally:

```bash
shoulders skill install
```

Or into the current workspace:

```bash
shoulders skill install --workspace
```

Then invoke via `/shoulders` in VS Code Copilot chat.
