# Contributing

See `CONTRIBUTING.md` in the repo root for development setup and guidelines.

Recommended local loop:

```bash
shoulders up --local
# ... edit 2-addons/... ...
shoulders sync
```

Develop per-profile with `small` on laptops; use `medium`/`large` for full-stack validation.
