apiVersion: apiextensions.crossplane.io/v2
kind: CompositeResourceDefinition
metadata:
  name: workspaces.shoulders.io
spec:
  group: shoulders.io
  names:
    kind: Workspace
    plural: workspaces
  scope: Cluster
  versions:
    - name: v1alpha1
      served: true
      referenceable: true
      schema:
        openAPIV3Schema:
          type: object
          properties:
            spec:
              type: object
              description: Workspace configuration. Currently no fields; the workspace name carries the tenant identity and drives the namespace, network policy and naming guardrails.
